import Component from 'react';
import { Point } from './GeoHashLib.js';
import

export class GeoHashConverter extends Component {
    render() {
        return (
            <div id="geo-hash-converter">
                <h3>Converts from a geohash to a latitude and longitude or converts from latitude and longitude to a geohash</h3>
                <div>

                </div>
            </div>
        )
    }
}